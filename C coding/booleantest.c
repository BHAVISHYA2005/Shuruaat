#include <stdio.h>
int main(){
    int myage = 23;
    int votingage =18;
    if (myage >= votingage){
        printf("eligible to vote");
    }else 
    {printf("not eligible to vote");
    }
}